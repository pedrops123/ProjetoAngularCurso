import { ExampleDivDirective } from './example-div.directive';

describe('ExampleDivDirective', () => {
  it('should create an instance', () => {
    const directive = new ExampleDivDirective();
    expect(directive).toBeTruthy();
  });
});
