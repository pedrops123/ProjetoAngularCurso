import { DirectiveBidingListenerDirective } from './directive-biding-listener.directive';

describe('DirectiveBidingListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveBidingListenerDirective();
    expect(directive).toBeTruthy();
  });
});
